============
Installation
============

At the command line::

    $ pip install emaillib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv emaillib
    $ pip install emaillib

Or, if you are using pipenv::

    $ pipenv install emaillib

Or, if you are using pipx::

    $ pipx install emaillib
